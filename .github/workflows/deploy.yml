name: Deploy Bildebank Worker

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Workers
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # --legacy-peer-deps: the sanity-bildebank workspace (studio plugin, not
      # part of the worker bundle) has a react 18 / react-dom 19 peer conflict.
      # `npm install` rather than `npm ci`: package-lock.json still lists the
      # removed @jsquash packages. Switch back to `npm ci` once the lockfile
      # has been regenerated locally.
      - name: Install dependencies
        run: npm install --legacy-peer-deps --no-audit --no-fund

      - name: Type check
        run: npx tsc --noEmit

      - name: Deploy to Cloudflare Workers
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
